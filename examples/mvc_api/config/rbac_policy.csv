# Policy for the mvc_api example: only the built-in health endpoint. The
# notes module carries its own rows (internal/notes/module.go), which is
# what makes Mount() the whole integration. Actions are the framework's
# CRUD verbs.
p, anonymous, /healthz, read, allow

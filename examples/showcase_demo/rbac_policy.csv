# Starter RBAC policy for the suite template — a default-deny Casbin
# enforcer is active, so anonymous requests need an explicit allow.
#
# This file grants anonymous access only to the built-in health endpoint.
# As you add public routes outside a module, add rows here. The action
# column uses the framework's CRUD verbs (GET/HEAD->read, POST->create,
# PUT/PATCH->update, DELETE->delete), NOT raw HTTP methods. Model: p = sub,
# obj, act, eft (keyMatch on obj; deny overrides allow).
p, anonymous, /healthz, read, allow

# The shop module carries its own rows for /api/authors and /api/articles
# (shop/module.go), and the orbit admin panel brings its own login gate under
# /admin: neither needs a row here. A module generated with `nucleus generate
# module <name>` carries its own rows and CSRF exemption the same way.

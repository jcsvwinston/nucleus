# Handoff — last session closing note

> Owned by `session-curator`. Overwritten at the end of every session
> by `/handoff`. Read first by `/resume` at the start of the next one.

ITERATION:    Integration sprint — wire up #40 (JWT rotation), #41 (Casbin deny-override) and #46 (circuit breaker) into the default `App.New` path; close the CHANGELOG honesty gap.
BRANCH:       main
LAST COMMIT:  b1e497e docs: sync internal docs/ with #38-#46 (post-iteration alignment) (#47)
STATUS:       not started — primitives shipped, framework still does not consume them by default. Operator who calls `app.New(cfg)` today does NOT get key rotation, default-deny RBAC, or circuit-broken external I/O without wiring them by hand. The CHANGELOG entries for #40/#41/#46 read as "feature shipped" but the surface is only the primitive.
NEXT STEP:    Open `feat/integration-sprint` and ship four things:
              (1) `App.New` builds `*auth.JWTManager` from a new `Config.Auth.JWTKeys[]` config schema (HS256 via `secret_env`, RS256 via `pem_path` / `pem_env`; fallback to legacy single-secret when slice is empty); auto-mount JWKS handler at `/.well-known/jwks.json` when at least one key is asymmetric; expose the manager on `*App` and consume it in the auth middleware path.
              (2) Casbin enforcer wired into the router by default. Seed an in-memory bootstrap policy that allows the always-unauthenticated routes (`/healthz`, `/metrics`, `/admin/login`, `/login`) before the middleware mounts. Log a WARN on empty user policy. `app.WithOpenAuthz()` opt-out with rationale captured in slog. Breaking change for examples without policies — document under CHANGELOG `Changed`.
              (3) `pkg/circuit` wrapping in `pkg/mail` (SMTP — SendGrid was the only other in-tree provider and has been removed in DEP-2026-002 / MA-2026-002; external plugins handle their own circuit-breaker concerns) and `pkg/storage` (S3 / GCS / Azure put/get/list/delete). Knobs at `Config.Mail.CircuitBreaker{...}` and `Config.Storage.CircuitBreaker{...}`; defaults `Enabled: true, FailureThreshold: 5, Cooldown: 30s, HalfOpenMaxConcurrent: 1`.
              (4) E2E tests covering the three: (a) token signed with a rotated-out kid still validates during the grace window after `RotateKey`; (b) request without a matching allow returns 403 against the wired middleware with no policy file loaded; (c) SMTP / S3 with N consecutive failures returns `circuit.ErrOpen` from the next call.
              CHANGELOG cleanup at the end of the iteration: #40 / #41 / #46 entries from "primitive added" → "primitive added + wired by default", with breaking-change notes against `App.New` for the Casbin default-deny path.
BLOCKERS:     None code-side. Pending decision: does the Casbin default-deny mount need its own ADR? My read is yes — it is a deliberate framework default that an enterprise comité will ask about. ADR-004 candidate.
FILES OF INTEREST: pkg/app/app.go (wire-up site), pkg/app/config.go (new `Auth.JWTKeys[]`, `Mail.CircuitBreaker`, `Storage.CircuitBreaker` schemas), pkg/auth/jwt.go, pkg/authz/middleware.go, pkg/mail/smtp.go + sendgrid.go, pkg/storage/s3.go + gcs.go + azure.go, pkg/circuit, examples/mvc_api (verify the seeded bootstrap allow-list keeps the example working), CHANGELOG.md, docs/adrs/ (for ADR-004 if we cut one), docs/guides/AUTH_GUIDE.md, docs/guides/OBSERVABILITY_BASELINE.md.
NOTES:        Critique that triggered this iteration (verbatim from owner, 2026-05-13): "pkg/auth.NewJWTManager no se invoca desde pkg/app, el middleware de Casbin no se monta sin archivo de política, pkg/circuit se importa solo desde markdown. CHANGELOG #40/#41/#46 está vendiendo features que el framework no consume." Three sharp edges flagged in the discussion before this handoff was written: (i) seed a bootstrap allow-list for never-gated routes so `examples/*` doesn't break; (ii) circuit-breaker autowrap needs a `Config.{Mail,Storage}.CircuitBreaker` knob so apps with low-traffic SMTP can opt out of the 5/30s default; (iii) JWT key config schema reuses the `CredentialSource` pattern (`pem_path` / `pem_env` / `secret_env`) so PEMs don't end up in tracked YAML. Once this iteration lands, the queue resumes with the user's prior P1 (schema-level drift detection + MSSQL/Oracle AutoMigrate scaffolds) OR P0 (ES256 / ECDSA + cloud secret managers) depending on owner direction. Open queue still has: SendGrid + external-plugin mail probes (deferred in #42), CSV migration helper for Casbin 4-column rows (deferred in #41), test rename TestSQLMatrix_Exploratory* → *_Live* (deferred in #38), pkg/admin/ui post-Phase 7 deprecation, docs/ vs website/docs/ overlap rationalization, v1.0 cut planning, Phase 4 modularization (post-v1).

Updated: 2026-05-13

# Nucleus against OWASP ASVS 4.0.3, level 2 — MEASURED, not transcribed.
# Regenerate: go test ./contracts/ -run TestASVSL2 -update-asvs
# met = the framework satisfies it · application = the framework provides
# the mechanism and the application must use it · not-met = it does not,
# with the reason in contracts/asvs_test.go.

V2.1.1   met          passwords of at least 12 characters are allowed and required
V2.1.2   met          passwords of at least 64 characters are accepted
V2.1.7   met          no composition rules are imposed
V2.2.1   met          anti-automation: repeated failures are throttled
V2.2.3   application  the user is notified of security-relevant changes
V2.3.1   met          initial secrets are generated by the system with 112+ bits
V2.5.1   met          a recovery mechanism does not reveal the current password
V2.5.6   met          recovery uses a securely generated random code, single-use and time-limited
V2.5.7   met          a recovery flow does not disclose whether an account exists
V2.7.2   not-met      out-of-band verifiers expire within 10 minutes
V2.8.1   met          time-based OTPs have a defined lifetime
V2.8.5   met          a one-time verifier is accepted only once
V3.2.1   met          a new session token is generated on authentication
V3.2.3   met          session tokens are stored in cookies with Secure, HttpOnly and SameSite
V3.3.1   met          logout invalidates the session
V3.3.2   application  an inactivity timeout is enforced
V3.3.4   met          the user can end sessions on other devices
V3.5.2   met          static API secrets are not used; tokens are revocable
V4.1.1   met          access control rules are enforced on a trusted service layer
V4.1.3   met          least privilege: access is denied by default
V4.2.1   met          sensitive operations require re-authentication or a second factor
V7.1.1   met          credentials are not written to the log
V14.4.1  met          responses declare a safe content type
V14.4.3  met          a Content-Security-Policy is sent
V14.4.4  met          X-Content-Type-Options: nosniff is sent

# met 22 · application 2 · not-met 1 · of 25

# Iteration: Get main CI green, then cut v0.8.0

> Archived: 2026-05-28.
> Status: COMPLETE — all acceptance criteria met; v0.8.0 released.

## Goal

Get `main` CI fully green (had been red since ~2026-05-24) and cut the parked
v0.8.0 release.

## Scope

1. Fix `cmd/nucleus/main_test.go`: 3 scaffold-layout assertions broken by the
   skeleton rework (`f073953`, 2026-05-25) + 1 OpenAPI title mismatch.
2. Root-cause the MSSQL live-smoke CI lane failure (real bug vs flake).
3. Push the two local commits once green:
   - `b829855` docs(governance): ADR-012 prometheus exporter + firewall
     hardening + CI_MATRIX truth-fix.
   - `217fed5` fix(db): split multi-statement AutoMigrate scripts for MySQL
     and SQLite (Error-1064 fix).
4. Resume parked v0.8.0 release:
   - Promote CHANGELOG `[Unreleased] → [0.8.0] - 2026-05-28`.
   - Regenerate `docs/reports/`.
   - Annotated `v0.8.0` tag + push.
5. Governance follow-up: enforce branch protection / required gate on `main`.

## Acceptance criteria (all met)

- [x] `go test ./...` passes locally with zero failures.
- [x] CI `Test And Smoke` lane green on `main` (govulncheck CVEs cleared, `544f39a`).
- [x] CI `DB Matrix Required (mysql)` lane green (VARCHAR + inline-index fixes).
- [x] CI `DB Matrix Live (mssql)` lane green (NVARCHAR fix).
- [x] Full CI green on `main` — run `26533028754` (commit `0eed39a`) concluded
      `success`, including the `CI Required Gate`.
- [x] CHANGELOG `[0.8.0]` section promoted and accurate (`ae394b7`).
- [x] `git tag v0.8.0` pushed; GitHub release object created at
      https://github.com/jcsvwinston/nucleus/releases/tag/v0.8.0 — marked
      Latest, not draft, not prerelease.

## Completion summary

### What shipped in this iteration

- **Fixed 4 stale `cmd/nucleus/main_test.go` tests** (`bf7b881`): accepted the
  module-path-derived OpenAPI title "Contractapp API"; updated scaffold-layout
  assertions to the empty-skeleton reality.
- **Fixed real `app.New` startup panic** (`d5c6203`): `template.Must(ParseGlob)`
  panicked when `TemplatesDir` had no `.html` files. Now parses only when ≥1
  match; genuine parse errors return via `wrapOp`. New
  `pkg/app/app_templates_test.go` covers both paths.
- **Cleared all red-CI causes → `main` fully green** (`0eed39a`):
  1. govulncheck CVEs — bumped `golang.org/x/net` v0.55, `otlpmetrichttp` v1.43,
     `go-jose/v4` v4.1.4 (`544f39a`).
  2. MySQL Error 1170 — key-bound string columns → `VARCHAR(255)` (`47dfce4`).
  3. MSSQL invalid-key-column — same root cause → `NVARCHAR(255)` (`47dfce4`).
  4. MySQL Error 1061 idempotency — indexes declared INLINE in `CREATE TABLE`
     so re-run `AutoMigrate` is a no-op (`0eed39a`). `go.work.sum` synced
     (`1dd469e`).
- **v0.8.0 released** (`ae394b7`):
  - CHANGELOG `[Unreleased] → [0.8.0] - 2026-05-28` promoted.
  - `docs/reports/` regenerated.
  - Annotated tag `v0.8.0` → commit `ae394b7`. IMMUTABLE — proxy.golang.org
    has already fetched v0.8.0 (HTTP 200). `go install
    github.com/jcsvwinston/nucleus/cmd/nucleus@v0.8.0` resolves.
  - GitHub release object created manually via `gh release create` (notes from
    CHANGELOG `[0.8.0]` section, 0 binary assets — notes-only).
- **Fixed stale `release.yml`** (`98753c8`): the verify job ran
  `node --check pkg/admin/ui/{components,app}.js` — files deleted when admin UI
  migrated to Vite/React/TS (`pkg/admin/ui/src/`). Failed MODULE_NOT_FOUND on
  every tag push; this is why v0.6.0 and v0.7.0 had no GitHub release object
  (last release object before v0.8.0 was v0.5.5). Fixed by removing the stale
  step and the now-unused Setup Node step from the verify job. The next tag
  (v0.8.1/v0.9.0) will publish cleanly via `release.yml`. Because the tag was
  already immutable, the GitHub release object for v0.8.0 was created manually.

### Key commits (chronological)

| Hash      | Subject                                                                 |
|-----------|-------------------------------------------------------------------------|
| `bf7b881` | test(cli): update stale scaffold/openapi tests to the skeleton layout   |
| `d5c6203` | fix(app): don't panic on an empty or malformed templates dir            |
| `544f39a` | fix(deps): bump x/net, otlpmetrichttp, go-jose to clear govulncheck CVEs|
| `47dfce4` | fix(model): bound key-bound string columns on MySQL and MSSQL           |
| `1dd469e` | chore: sync go.work.sum after dependency bumps                          |
| `0eed39a` | fix(model): declare MySQL indexes inline so AutoMigrate is idempotent   |
| `ae394b7` | docs(release): v0.8.0 prep — promote CHANGELOG + release reports        |
| `98753c8` | fix(ci): drop stale admin-UI JS syntax check from release workflow      |

### Final git state

- Branch: `main`. HEAD: `98753c8`. Clean. In sync with `origin/main`.
- `main` CI run for `98753c8` in progress at archive time — workflow-file-only
  change over code that was green at `ae394b7`; expected green.

## Carry-forward backlog (seed for next iteration)

### Framework bugs (P1/P2)

- **P1 — `WithoutDefaults()` leaks the admin bootstrap user.**
  `pkg/app/app.go:~272` calls `admin.EnsureBootstrapAdminUser`
  UNCONDITIONALLY, before the `if !o.skipDefaults` guard. Fix: move call
  inside the `!o.skipDefaults` branch.

- **P2 — `Router.Resource("")` under a module `Prefix` panics at startup.**
  `pkg/nucleus/router.go` — `joinPath` should yield `/` (not `""`) when
  prefix+path are both empty.

### ADR-010 §2 layer 5 (module-specific config binding/validation)

Completes the five-layer validator; layer 4 (referential) shipped 2026-05-26.

### Governance

- **Branch protection / required gate is NOT enforced on `main`.** We pushed
  directly to main twice this session (release prep + workflow fix). Red CI can
  still be pushed. Must be fixed before the next red CI event causes the same
  damage.

### Other carry-forward

- Cloud Secrets Provider plugin extraction (AWS/GCP/Azure/Vault out of core).
- SchemaDrift column-type comparison + `docs/guides/MODELING_MULTI_DATABASE.md`.
- `go mod tidy` unblock — resolve the `admin/proto` replace-directive.
- `tasks.Manager` struct→interface DEP (optional DEP-2026-004).
- Audit §7 minors: 503 path test for `/healthz`; endpoints-parity doc-parsing;
  `pkg/health/{db,redis,storage}.go` tests.

### Deferred carry-forwards (not blocking)

_Oracle multi-block AutoMigrate (2026-05-24):_
- Route admin-bootstrap PL/SQL through `db.ExecScript`. (architect NIT.)
- Oracle DDL auto-commit vs the Migrator transaction.

_session_cookie_secure (2026-05-23):_
- Startup validation: `SameSite=None` requires `Secure=true`. (security-auditor
  LOW.) `pkg/auth/session.go` should reject the combo.

_Oracle identifier-casing (2026-05-23):_
- CI governance reconciliation (mssql + oracle): required vs exploratory.
- Oracle reserved-word + dotted-identifier hardening.

_Phase 3b / observability (2026-05-22/23):_
- GCS credential-redaction forward-compat.
- Reverse-proxy hardening note for `/_/config`.
- Relocate `pkg/observability` to `internal/` post-v1.0.
- `Runtime.AutoMigrate` production guard.

_ADR-010 Phase 1 (still open):_
- Service-shutdown timeout — `nucleus.Run`'s `wg.Wait()` has no deadline.
- `Lifecycle.OnShutdown` context deadline — no bound.
- `joinPath` double-slash collapse — `routerAdapter.joinPath`.

_Internal-docs (low-priority):_
- `DETAILED_TUTORIAL.md` flat-handler style predates `nucleus.Module` pattern.
- `DEVELOPER_MANUAL.md §5.3` references `internal/contracts`.

# Iteration: Cut v0.9.0

> Archived: 2026-06-09.
> Status: COMPLETE — all validation gates passed; v0.9.0 released.

## Goal

Run full `/release-prep` validation, then cut and publish the v0.9.0 release
(audit-remediation + security-hardening + docs-faithfulness CI milestone).

## Scope

This iteration ran without a pre-seeded `CURRENT_ITERATION.md` (the file was
at the empty template throughout). Acceptance criteria below are reconstructed
from the actual work completed, not declared in advance.

## Acceptance criteria (all met)

- [x] `/release-prep` full-strength governance pass with zero unresolved blockers.
- [x] Contract freeze checks PASS.
- [x] Compatibility harness READY 100% (8/8 lanes).
- [x] Dependency impact scan NO CRITICAL.
- [x] Website coverage strict PASS including §9 body-content check.
- [x] CHANGELOG `[Unreleased]` promoted to `[0.9.0] - 2026-06-09` with required
      `### Compatibility statement` section.
- [x] Annotated tag `v0.9.0` pushed; GitHub release workflow completed `success`;
      release published with 7 assets.
- [x] `defaultPinnedFrameworkVersion` in `internal/cli/new.go` bumped v0.8.0 → v0.9.0.
- [x] Real-user-path smoke verified against the published module (both mvc and api
      templates, no `replace` directives).

## Completion summary

### Release-prep validation results

- **Governance (full-strength):** 1 blocker found during the sweep — missing
  `### Compatibility statement` in CHANGELOG `[Unreleased]`. Resolved before
  cutting the tag (added in PR #109).
- **Contract freeze:** PASS.
- **Compatibility harness:** READY 100% (8/8 lanes).
- **Dependency impact:** NO CRITICAL.
- **Website coverage strict (including §9 body-content):** PASS.
- **Deprecation / migration-assistant coupling:** PASS.
  - DEP-2026-002 corrected: `Announced in` / `Earliest removal` fields had
    `Unreleased` → corrected to `v0.7.0`.

### Stale-state correction

The prior handoff (written at commit `807d50b`, 2026-06-09) described a
"parked v0.8.0 release" as the next strategic item. In reality v0.8.0 was
already tagged on 2026-05-28 and published. The active target was v0.9.0.
Memory and handoff were corrected in this session.

### What shipped

**PR #109 (`929234e`) — `docs(release): v0.9.0 prep — promote CHANGELOG + release reports`**

- CHANGELOG `[Unreleased]` → `[0.9.0] - 2026-06-09`.
- `### Compatibility statement` added (required section, was the blocker):
  - SEC-1 CORS flip: `corsAllowCredentials` default `true` → `false` — BREAKING-operational.
  - `authz.Enforcer` clean break (new interface; old struct removed).
  - Two stricter-at-startup validations (model column-tag + join-path).
  - Zero stable symbol removals.
- Release artifacts committed:
  - `docs/reports/compatibility_report_2026-06-09.md` — READY.
  - `docs/reports/dependency_impact_2026-06-09.md` — NO CRITICAL.
- DEP-2026-002 `Announced in` / `Earliest removal` corrected (`Unreleased` → `v0.7.0`).

**Tag `v0.9.0`**

- Annotated tag pushed from commit `929234e` with explicit maintainer authorization.
- Release workflow (verify + GoReleaser publish) status: `success`.
- 7 binary/source assets published.
- Release title: "v0.9.0 — audit remediation: portable CRUD, security hardening + docs-faithfulness CI".

**PR #110 (`f546f52`) — `chore(cli): pin scaffold to the published v0.9.0 release`**

- `defaultPinnedFrameworkVersion` bumped v0.8.0 → v0.9.0 in `internal/cli/new.go`.
- Fresh `[Unreleased]` entry added to CHANGELOG (pin bump record).

### Real-user-path smoke (both templates)

Executed against the published module (no `replace` directives):

- `nucleus new <name> --template mvc` and `nucleus new <name> --template api`.
- Generated `go.mod` pins: `v0.9.0` / `go 1.26.4` / no `toolchain` directive.
- `go mod tidy` downloaded the published module via the Go proxy without error.
- `go build ./...` succeeded. App boot succeeded.
- `/healthz` → 200 OK.
- `/admin` → 302 → login page.
- Unauthenticated `GET /admin/api/rbac/policies` → 401 (ADR-016 router-edge
  authentication verified on the published release).
- Bootstrap admin password printed once to stderr on first boot.
- `api`/core-only template (WithoutDefaults): ZERO admin bootstrap output
  (WithoutDefaults fix from the remediation queue verified live).
- Zero frictions found.

### Key commits (chronological)

| Hash      | PR   | Subject                                                                   |
|-----------|------|---------------------------------------------------------------------------|
| `929234e` | #109 | docs(release): v0.9.0 prep — promote CHANGELOG + release reports         |
| `f546f52` | #110 | chore(cli): pin scaffold to the published v0.9.0 release                 |

### Final git state

- Branch: `main`. HEAD: `f546f52`. Clean. In sync with `origin/main`.
- v0.9.0 is the latest published release on pkg.go.dev and the GitHub Releases page.

## Carry-forward backlog (seed for next iteration)

### Prototype phase (strategic)

Per the agreed plan: **prototype #1 pinned to v0.9.0**. Distilled prototypes
later become ADR-010 Phase 4 Slice 2/3 reference apps + harness fixture profiles.
ADR-010 Phase 4 Slices 2/3 target the v0.9.X range.

### Known cautions for prototype work

- `Module.Jobs` / `Module.Webhooks` are reserved shape (boot-WARN, not executed).
  Use `pkg/tasks` directly for background jobs.
- `outbox.NewKafkaBridge` is deliberately unfinished.

### Housekeeping (non-blocking)

- **F-13 (P3):** `CLAUDE.md` §directory-map still says `cmd/goframe/` — actual
  entry-point is `cmd/nucleus/`. Fix opportunistically in any docs PR.
- **Deletable untracked artifact:** `docs/audits/2026-06-07-exhaustive-audit.md`
  is superseded and not in git. Delete at maintainer's discretion.

### Framework bugs (carry-forward from v0.8.0 iteration, still open)

- **P2 — `Router.Resource("")` under a module `Prefix` panics at startup.**
  `pkg/nucleus/router.go` — `joinPath` should yield `/` (not `""`) when
  prefix + path are both empty.

### Deferred carry-forwards (not blocking)

_ADR-010 Phase 1 (open):_
- Service-shutdown timeout — `nucleus.Run`'s `wg.Wait()` has no deadline.
- `Lifecycle.OnShutdown` context deadline — no bound.

_Oracle/MSSQL (deferred per CI_MATRIX):_
- Oracle reserved-word + dotted-identifier hardening.
- Oracle DDL auto-commit vs the Migrator transaction.

_Observability / security (low-priority):_
- GCS credential-redaction forward-compat.
- Reverse-proxy hardening note for `/_/config`.
- `SameSite=None` requires `Secure=true` — startup validation in `pkg/auth/session.go`.
- Relocate `pkg/observability` to `internal/` post-v1.0.

_Internal-docs (low-priority):_
- `DETAILED_TUTORIAL.md` flat-handler style predates `nucleus.Module` pattern.
- `DEVELOPER_MANUAL.md §5.3` references `internal/contracts`.
